// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for RegisterWebhookFunction
 */
export interface RegisterWebhookFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/register-webhook.
 */
export class RegisterWebhookFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: RegisterWebhookFunctionProps) {
    super(scope, id, {
      description: 'src/register-webhook.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs16.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../assets/register-webhook.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}